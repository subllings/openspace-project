# Core dependencies
flask>=2.3,<3.0

# For development and testing
unittest2; python_version < "3.11"

# Optional: tools for formatting/logging/debugging (facultatif)
rich>=13.0  # for pretty console output

# Test runner framework (stable & compatible with Python 3.11–3.13)
pytest>=7.4,<8.0
pytest-cov>=4.1,<5.0   # Plugin pytest to measure test coverage